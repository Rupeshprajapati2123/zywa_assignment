
const { DeliveredSchema, PickupSchema, ReturnedSchema } = require('./schemas');

  const a = ["User's card was Returned."
            ,"User's Card was ",
             "User's Card was not delivered.",
             "Card is out for delivery."];
  
module.exports={a};  